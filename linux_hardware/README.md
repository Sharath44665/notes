## Run Hardware Diagnostics

- Check System Resources: Use tools like `htop`, `lscpu`, and `lsusb` from the terminal to check system performance and resource usage.
- Inspect Drivers: Ensure drivers for your hardware (like graphics and network) are loaded correctly using the `lspci` and `lsmod` commands.

